# dunglas/symfony-docker

⬅️ [README](../README.md)

## `/var` directory does not sync

For the moment, move `VOLUME /app/var/` in the `frankenphp_prod` part of the `Dockerfile`.

See https://github.com/dunglas/symfony-docker/issues/489.
